import { CryptoCurrencyExchangeInterface } from 'interfaces/crypto-currency-exchange';
import { UserInterface } from 'interfaces/user';
import { GetQueryInterface } from 'interfaces';

export interface OrganizationInterface {
  id?: string;
  description?: string;
  image?: string;
  name: string;
  created_at?: any;
  updated_at?: any;
  user_id: string;
  tenant_id: string;
  crypto_currency_exchange?: CryptoCurrencyExchangeInterface[];
  user?: UserInterface;
  _count?: {
    crypto_currency_exchange?: number;
  };
}

export interface OrganizationGetQueryInterface extends GetQueryInterface {
  id?: string;
  description?: string;
  image?: string;
  name?: string;
  user_id?: string;
  tenant_id?: string;
}
